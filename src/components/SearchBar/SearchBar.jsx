import React from 'react';

const SearchBar = () => {
  return (
    <div>
      <input type="text" placeholder="e.g. Kyiv" />
      <button>Search</button>
    </div>
  );
};

export default SearchBar;
